# FailureLudo — Claude instructions

Read and follow [AGENTS.md](AGENTS.md), the shared project instructions, once if it
is not already in context. Load its references only when relevant to the request.
Do not automatically scan the repository or read every plan at session startup.
